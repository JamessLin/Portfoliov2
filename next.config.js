/** @type {import('next').NextConfig} */
const nextConfig = {
    eslint: {
      // Warning: Disabling ESLint during builds may hide errors.
      ignoreDuringBuilds: true,
    },
  };
  
  module.exports = nextConfig;
  